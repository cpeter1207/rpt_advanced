name: Required quality
on:
  push:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
jobs:
  quality:
    uses: cpeter1207/rpt_advanced-workflows/.github/workflows/quality.yml@main
